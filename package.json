{
  "name": "life-giving-foundation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd website && npm ci && npm run build",
    "dev": "cd website && npm run dev"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
